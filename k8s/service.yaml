apiVersion: v1
kind: Service
metadata:
  name: expense-tracker
  labels:
    app: expense-tracker
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: expense-tracker
